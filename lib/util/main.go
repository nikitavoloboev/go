package util

import (
	"strconv"
)

// TODO: just a test function, create useful utility functions
// make it work with deploy of pulse http server
func ConvertToString(i int) string {
	return strconv.Itoa(i)
}
